import Filters from '../filters/filters';

/**
 * filters / book cards
 * @returns content after main banner
 */
const MainPage = () => {
  return (
    <>
      <Filters />
    </>
  );
};

export default MainPage;
